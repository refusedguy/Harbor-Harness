In the workspace directory there are two Python files: `shout.py`
(a tiny CLI script) and `test_shout.py` (its unit tests, currently
FAILING).

`shout.py` crashes on empty input. Fix it so that all tests in
`test_shout.py` pass. You may run the tests yourself to iterate:

```sh
python3 -m unittest -v
```

Do NOT modify `test_shout.py`. Do not create any other files.
