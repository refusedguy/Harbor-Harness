In the workspace directory there is a folder `data/` with exactly three
files: `zebra.txt`, `apple.txt`, `mango.txt` (contents do not matter).

Create a file `manifest.txt` in the workspace root listing exactly those
three file names (base names only, no paths), one per line, sorted
alphabetically:

```text
apple.txt
mango.txt
zebra.txt
```

Do not modify anything inside `data/`. Do not create any other files.
