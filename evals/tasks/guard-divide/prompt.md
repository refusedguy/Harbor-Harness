In the workspace directory there is a Python file `calc.py` with a function
`divide(a, b)` that currently raises `ZeroDivisionError` on `b == 0`.

Change `divide` so that division by zero returns `0` instead of raising.
Keep the normal division behavior unchanged. Do not add dependencies.
Do not create any other files.
