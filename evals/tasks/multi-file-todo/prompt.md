In the workspace directory there are three text files: `a.txt`, `b.txt`,
and `c.txt`. Each file contains exactly one line with the placeholder
text `PLACEHOLDER`.

Replace `PLACEHOLDER` with `DONE` in all three files. Nothing else in the
files may change. Do not create any other files.
