In the workspace directory there is a file `config.json`:

```json
{
  "host": "localhost",
  "port": 8080,
  "debug": false
}
```

Change `port` to `9090`. Keep every other key and value exactly as-is
(same formatting is not required, but no keys may be added, removed, or
reordered in meaning). Do not create any other files.
