In the workspace directory there are two Python files: `cart.py` (a tiny
checkout helper) and `test_cart.py` (its unit tests, currently FAILING).

Find the bug in `cart.py` and fix it so that all tests in
`test_cart.py` pass. You may run the tests yourself to iterate:

```sh
python3 -m unittest -v
```

Do NOT modify `test_cart.py`. Do not create any other files.
