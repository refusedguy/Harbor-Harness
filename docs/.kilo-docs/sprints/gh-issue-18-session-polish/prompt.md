# SPRINT: gh-issue-18-session-polish

## Goal
Session polish: /tree, branch summaries, JSONL import/export, rename.

## Context
Сессии требуют лучшей навигации и экспорта для power users.

## Tasks
- /tree команда для сессий
- Branch summaries в session header
- JSONL import/export
- Переименование сессий

## Acceptance
- /tree отображает дерево сессий
- JSONL экспорт/импорт без потерь
- Rename через /rename или UI
