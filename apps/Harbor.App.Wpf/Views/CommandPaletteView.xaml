<Window x:Class="Harbor.App.Wpf.Views.CommandPaletteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Harbor.App.Wpf.ViewModels"
        Title="Command Palette"
        Background="{DynamicResource App.Surface}"
        Foreground="{DynamicResource App.Text}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height"
        KeyDown="Window_KeyDown">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 x:Name="QueryBox"
                 Background="{DynamicResource App.Background}"
                 Foreground="{DynamicResource App.Text}"
                 BorderBrush="{DynamicResource App.Accent}"
                 BorderThickness="2"
                 Padding="10,8"
                 FontSize="14"
                 Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                 Loaded="QueryBox_Loaded"/>

        <ListBox Grid.Row="1"
                 Background="Transparent"
                 Foreground="{DynamicResource App.Text}"
                 BorderThickness="0"
                 MaxHeight="320"
                 SelectedIndex="{Binding SelectedIndex}"
                 ItemsSource="{Binding Results}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:CommandEntry}">
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Category}"
                                   Foreground="{DynamicResource App.TextMuted}"
                                   FontSize="11" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource App.Text}"/>
                            <TextBlock Text="{Binding Description}"
                                       Foreground="{DynamicResource App.TextMuted}" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
